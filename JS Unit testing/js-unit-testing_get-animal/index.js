export default function getAnimal(argument) {
if (argument === "cats") return ("I totally love cats!");
else if (argument === "animalPluralName") return ("I like dogs!");
else if (!argument) return ("I do not like animals at all!");
}